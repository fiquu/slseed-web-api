type User {
  _id: ID!
  sub: String!
}

type Query {
  users(skip: Int, limit: Int): [User!]!
  user: User!
}

type Mutation {
  createUser(sub: String!): User
  updateUser(_id: ID!, sub: String!)
}
